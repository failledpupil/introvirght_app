{
  "name": "introvirght-vercel",
  "version": "1.0.0",
  "description": "Introvirght Vercel deployment",
  "main": "server/api/index.js",
  "scripts": {
    "build": "cd introvirght && npm install && npm run build",
    "dev": "cd introvirght && npm run dev"
  },
  "dependencies": {
    "@vercel/node": "^3.0.0",
    "@types/node": "^20.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}